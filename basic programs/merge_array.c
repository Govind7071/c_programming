#include<stdio.h>
void mergesortedarrys(int a[],int n,int b[],int m,int c[],int p)
{
    int i=0,j=0,k=0;
    while(i<n&&j<m){
    if(a[i]<b[j])
    c[k++]=a[i++];
    else 
    c[k++]=a[i++];
}
while(i<n)
c[k++]=a[i++];
while(j<m)
c[k++]=b[j++];
}
int main ()
{
    int a,n,b,m,c,p;

   void mergesortedarrys(a[],n,b[],m,c[],p) 
   printf()
}